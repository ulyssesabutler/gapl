function unary() i: wire[32] => o: wire[32] {
    i => bitwise_not(32) => o;
}

function test() i: wire[32] => o: wire[32] {
    i   => unary()
        => register(wire[32])
        => unary()
        => unary()
        => unary()
        => o;
}
